import { StyleSheet } from "react-native";

export default StyleSheet.create({
  title: {
    textAlign: "center",
    fontSize: 28,
    fontWeight: "bold",
    marginTop: 50,
    marginBottom: 15,
  },
  error: {
    marginBottom: 10,
    paddingHorizontal: 20,
    fontWeight: "bold",
    color: "#f00",
  },
  button: {
    marginHorizontal: 12,
  },
});
